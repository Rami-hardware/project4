import { urlChecker } from "../js/urlChecker"

describe('Test "validateRequest()" should be a function' , () => {
    test('It should be a function', async () => {
        expect(typeof vUrl).toBe("undefined");
    });
});